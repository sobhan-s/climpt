apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: climpt
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      evaluation_interval: 15s

    scrape_configs:

      - job_name: 'climpt-worker'
        kubernetes_sd_configs:
          - role: pod
            namespaces:
              names: ['climpt']
        relabel_configs:
          - source_labels: [__meta_kubernetes_pod_label_app]
            action: keep
            regex: worker

      - job_name: 'climpt-aggregator'
        kubernetes_sd_configs:
          - role: pod
            namespaces:
              names: ['climpt']
        relabel_configs:
          - source_labels: [__meta_kubernetes_pod_label_app]
            action: keep
            regex: aggregator

      - job_name: 'climpt-jobsubmitter'
        kubernetes_sd_configs:
          - role: pod
            namespaces:
              names: ['climpt']
        relabel_configs:
          - source_labels: [__meta_kubernetes_pod_label_app]
            action: keep
            regex: jobsubmitter

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: climpt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus
  template:
    metadata:
      labels:
        app: prometheus
    spec:
      containers:
        - name: prometheus
          image: prom/prometheus
          args:
            - '--config.file=/etc/prometheus/prometheus.yml'
          ports:
            - containerPort: 9090
          volumeMounts:
            - name: config
              mountPath: /etc/prometheus
      volumes:
        - name: config
          configMap:
            name: prometheus-config

---
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: climpt
spec:
  type: NodePort
  ports:
    - port: 9090
      nodePort: 30090
  selector:
    app: prometheus
